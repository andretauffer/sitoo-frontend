{
  "name": "sitoo-cors-server",
  "dependencies": {
    "cors-anywhere": "^0.4.1"
  },
  "devDependencies": {},
  "scripts": {
    "client": "cd client && npm run start",
    "server": "cd cors-server && node cors-server.js",
    "mock-users": "cd client && node CreateMockUsers.js",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  }
}
